import ScriptsSection from '@/components/scripts/ScriptsSection';

/**
 * Scripts page, used to manage and edit scripts in the Workspace
 *
 * This is just a page route that contains the {@link ScriptSection} component
 */
export default async function ScriptsPage() {
  return <ScriptsSection />;
}
