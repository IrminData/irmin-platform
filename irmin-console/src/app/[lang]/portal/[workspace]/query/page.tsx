import QuerySection from '@/components/query/QuerySection';

/**
 * Query page, used to run Irmin SQL queries on workspace data
 *
 * This is just a page route that contains the {@link QuerySection} component
 */
export default function QueryPaage() {
  return <QuerySection />;
}
